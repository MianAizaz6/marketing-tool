const DashboardSubHeading = ({ subheading }: { subheading: string }) => {
  return <h2 className="text-[#333333] text-[14px] font-semibold leading-[136%]">{subheading}</h2>;
};

export default DashboardSubHeading;
